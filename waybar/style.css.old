window#waybar {
    font-family: "JetBrainsMono Nerd Font", "Font Awesome 6 Free", sans-serif;
    font-size: 18px;
    font-weight: bold;
}

#waybar {
    background-color: #1E1E2E;
    color: #CDD6F4;
    border: none;
    border-radius: 0;
}

#network,
#bluetooth,
#battery,
#tray,
#workspaces,
#pulseaudio,
#clock,
#custom-power {
    padding: 6px 0px;
    margin: 4px 0px;
}

#network {
    color: #ABE9B3;
    /* Green when connected */
    margin-right: 8px;
}

#network.disconnected {
    color: #F38BA8;
    /* Red when on but not connected */
}

#bluetooth.on {
    color: #F38BA8;
    /* Red when on but not connected */
}

#bluetooth.connected {
    color: #89B4FA;
    /* Blue when connected */
}

#bluetooth.off,
#bluetooth.disabled {
    color: #45475A;
    /* Dark gray when off */
}

/* --- THIS BLOCK IS UPDATED --- */
#battery {
    color: #F9E2AF;
    /* Yellow (discharging) */
    margin-right: 0px;
}

#battery:not(.charging):not(.plugged) {
    margin-right: 8px;
}

#battery.charging {
    color: #ABE9B3;
    /* Green (actively charging) */
}

#battery.plugged,
#battery.full {
    color: #CDD6F4;
    /* White (on AC, not charging) */
}

#battery.warning {
    color: #FAB387;
    /* Orange */
}

#battery.critical {
    color: #F38BA8;
    /* Red */
}
/* --- END OF UPDATE --- */

#tray {
    padding: 6px 0px;
}

#workspaces {
    padding: 10px 0;
    margin-right: 2px;
}

#workspaces button {
    padding: 5px 0px;
    color: #45475A;
    background: transparent;
    border: none;
    border-radius: 0;
}

#workspaces button.active {
    color: #89B4FA;
}

#workspaces button.urgent {
    color: #F38BA8;
}

#pulseaudio {
    color: #F38BA8;
    font-size: 20px;
}

#pulseaudio.muted {
    color: #6C7086;
}

#clock {
    background-color: #000000;
    color: #E0E0E0;
    border-radius: 8px;
    margin: 6px 4px;
    padding: 8px 0px;
    font-size: 22px;
    font-weight: 700;
    border: 1px solid #333333;
}

/* --- NEW BLOCK --- */
#custom-power {
    background-color: #000000;
    color: #F38BA8;
    border-radius: 8px;
    margin: 6px 4px;
    padding: 6px 0px;
    font-size: 20px;
    border: 1px solid #333333;
}
